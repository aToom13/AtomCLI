name: atomcli_companion
description: "AtomCLI Mobile Companion App"
publish_to: "none"

version: 3.4.2+30402

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.6.1

  # WebSocket
  web_socket_channel: ^3.0.2

  # Secure storage (private key)
  flutter_secure_storage: ^9.2.4

  # Encrypted local cache and durable safe outbox
  cryptography: ^2.7.0
  sqflite: ^2.4.2

  # QR code scanning
  mobile_scanner: ^7.0.1

  # ED25519 signing — raw format compatible with Node.js crypto
  ed25519_edwards: ^0.3.1

  # In-app notifications (permission requests)
  flutter_local_notifications: ^19.3.0
  atomcli_android_live_updates:
    path: packages/android_live_updates

  # HTTP pairing handshake
  http: ^1.4.0
  intl: ^0.20.2
  flutter_markdown: ^0.7.7+1
  flutter_background_service: ^5.1.0
  file_picker: ^11.0.3
  path_provider: ^2.1.6
  open_filex: ^4.7.0
  share_plus: ^12.0.2
  url_launcher: ^6.3.2
  flutter_inappwebview: ^6.1.5
  mime: ^2.0.0
  image_picker: ^1.2.3
  speech_to_text: ^7.4.0
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  sqflite_common_ffi: ^2.3.6

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
